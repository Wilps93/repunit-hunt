//! Ситовые стадии CPU.
//!
//! * [`kbase`] — генератор простых показателей k (сегментное сито Эратосфена);
//! * [`small`] — отсев кандидатов по малым делителям q (до `bitsieve_q_limit`).
//!
//! Обе стадии полностью на CPU и снимают ~90% работы до того, как кандидат
//! попадёт на GPU, — см. комментарий к [`small::SmallSieve`] о том, почему
//! перебор ведётся «со стороны q», а не «со стороны k».

pub mod kbase;
pub mod small;

pub use kbase::PrimeIter;
pub use small::SmallSieve;
