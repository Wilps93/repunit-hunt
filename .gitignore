# ── Сборка Rust ───────────────────────────────────────────
/target/

# ── Побочные файлы LaTeX (сами paper_*.pdf и figs/ — в репозитории) ──
/paper/*.aux
/paper/*.log
/paper/*.out
/paper/*.toc
/paper/*.fls
/paper/*.fdb_latexmk
/paper/*.synctex.gz
# Журналы прогонов проверки (paper/verify*/**.log) — это протокол, не мусор.

# ── Ранняя сборка статьи, попавшая в каталог случайно ─────
/1 (1).pdf

# Промежуточные выводы check_stability.py (пять зёрен генератора)
/paper/results_seed*.txt

# ── ОС и редакторы ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Артефакт прерванной загрузки OEIS (страница редиректа, 41 байт)
/paper/data/.txt
